import { Dimensions } from 'react-native';

const { width } = Dimensions.get('window');
const USER_DATA_KEY = 'dolist_user_data';
const USER_SETTINGS_KEY = 'dolist_user_settings';

export default {
  width,
  USER_DATA_KEY,
  USER_SETTINGS_KEY,
};
